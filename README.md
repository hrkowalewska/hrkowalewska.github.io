# Helen's Site

The academic theme is a bit temperamental. The site currently works with hugo v0.116.1 and could
break with any other version.

Pushing to `master` builds and deploys the site to GitHub Pages automatically. See `CLAUDE.md` for
the local dev server and the deployment details.